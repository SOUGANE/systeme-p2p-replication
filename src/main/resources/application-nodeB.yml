server:
  port: 5001

node:
  storage: storage_node_5001
  id: http://localhost:5001
  peers:
    - http://localhost:5000
    - http://localhost:5002


logging:
  pattern:
    console: "%d{HH:mm:ss} %clr([%level]) %clr(%msg){faint}%n"


spring:
  output:
    ansi:
      enabled: ALWAYS